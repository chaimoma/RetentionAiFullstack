version: "3.9"

services:
  db:
    image: postgres:15
    container_name: retention-db
    environment:
      POSTGRES_USER: retention_user
      POSTGRES_PASSWORD: 12345
      POSTGRES_DB: retentionai
    ports:
      - "5433:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

  backend:
    build:
      context: ./backend
    container_name: retention-backend
    env_file:
      - ./backend/.env
    depends_on:
      - db
    ports:
      - "8081:8081"

  frontend:
    build:
      context: ./frontend
    container_name: retention-frontend
    depends_on:
      - backend
    ports:
      - "5501:3001"
    volumes:
      - ./frontend:/app 

volumes:
  db_data: